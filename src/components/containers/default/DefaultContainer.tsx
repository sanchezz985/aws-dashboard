import * as React from "react";
import style from "./default.module.scss";

const DefaultContainer: React.FC = () => {
    return <div className={style.defaultContainer} />;
};

export default DefaultContainer;